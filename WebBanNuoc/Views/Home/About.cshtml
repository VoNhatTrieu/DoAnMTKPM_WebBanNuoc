@{
    ViewBag.Title = "Về Sip & Savor";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<style>
    /* --- 1. BIẾN MÀU SẮC --- */
    :root {
        --brand-orange: #ff6b35;
        --brand-blue: #4a90e2;
        --bg-cream: #fff9f5;
        --text-dark: #2c3e50;
        --text-grey: #666666;
    }

    /* --- 2. HERO SECTION (Đã chỉnh sửa cho gọn hơn) --- */
    .about-hero {
        /* Giảm chiều cao từ 450px xuống 280px */
        min-height: 280px;
        /* Vẫn giữ căn giữa */
        display: flex;
        align-items: center;
        justify-content: center;
        flex-direction: column;
        background: linear-gradient(180deg, #fffbf7 0%, #fff0e3 100%);
        text-align: center;
        margin-bottom: 0;
        padding: 20px 0; /* Padding nhỏ để an toàn */
    }

        .about-hero h1 {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            font-weight: 800;
            font-size: 3rem; /* Giảm size chữ xuống xíu cho cân đối */
            color: var(--text-dark);
            margin-bottom: 10px;
            letter-spacing: -1px;
            line-height: 1.2;
        }

        .about-hero .highlight {
            color: var(--brand-orange);
            position: relative;
            display: inline-block;
        }

            /* Gạch chân trang trí */
            .about-hero .highlight::after {
                content: '';
                position: absolute;
                bottom: 6px;
                left: 0;
                width: 100%;
                height: 12px;
                background-color: rgba(255, 107, 53, 0.15);
                z-index: -1;
                border-radius: 4px;
                transform: rotate(-2deg);
            }

        .about-hero .lead {
            font-size: 1.1rem;
            font-weight: 500;
            color: var(--text-grey);
            max-width: 700px;
            margin: 0 auto;
        }

    /* --- 3. CÁC PHẦN CÒN LẠI (Giữ nguyên vẻ đẹp) --- */
    .section-padding {
        padding: 80px 0; /* Giảm khoảng cách giữa các section cho đỡ rời rạc */
    }

    .rounded-img {
        width: 100%;
        height: 480px; /* Điều chỉnh chiều cao ảnh cho cân đối */
        object-fit: cover;
        border-radius: 24px;
        box-shadow: 0 20px 40px rgba(0,0,0,0.1);
    }

    .sub-heading {
        font-size: 0.9rem;
        font-weight: 700;
        letter-spacing: 2px;
        color: #999;
        margin-bottom: 10px;
        display: block;
    }

    .main-heading {
        font-size: 2.8rem;
        font-weight: 800;
        line-height: 1.2;
        margin-bottom: 1.5rem;
        color: var(--text-dark);
    }

    .description-text {
        font-size: 1.1rem;
        color: var(--text-grey);
        margin-bottom: 2rem;
        line-height: 1.7;
    }

    .feature-list {
        list-style: none;
        padding: 0;
        margin-bottom: 40px;
    }

        .feature-list li {
            font-size: 1.1rem;
            margin-bottom: 15px;
            display: flex;
            align-items: center;
            font-weight: 500;
            color: #444;
        }

        .feature-list i {
            color: var(--brand-orange);
            font-size: 1.3rem;
            margin-right: 15px;
        }

    /* Buttons */
    .btn-custom {
        padding: 14px 35px;
        border-radius: 10px;
        font-weight: 700;
        text-decoration: none;
        transition: all 0.3s ease;
        display: inline-block;
    }

    .btn-orange {
        background-color: var(--brand-orange);
        color: #fff;
        border: 2px solid var(--brand-orange);
        box-shadow: 0 10px 20px rgba(255, 107, 53, 0.2);
    }

        .btn-orange:hover {
            background-color: #e85a2d;
            transform: translateY(-3px);
            color: white;
        }

    .btn-outline {
        background-color: transparent;
        color: var(--brand-blue);
        border: 2px solid var(--brand-blue);
        margin-left: 15px;
    }

        .btn-outline:hover {
            background-color: var(--brand-blue);
            color: #fff;
            transform: translateY(-3px);
        }

    /* Values Section */
    .values-section {
        background-color: var(--bg-cream);
    }

    .value-card {
        background: #fff;
        padding: 40px 30px;
        border-radius: 20px;
        text-align: center;
        height: 100%;
        border: 1px solid rgba(0,0,0,0.02);
        transition: all 0.3s ease;
        box-shadow: 0 5px 15px rgba(0,0,0,0.03);
    }

        .value-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 20px 40px rgba(0,0,0,0.08);
        }

    .icon-box {
        width: 80px;
        height: 80px;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 0 auto 25px;
        font-size: 32px;
    }

    .bg-light-orange {
        background-color: #fff0e6;
        color: var(--brand-orange);
    }

    .bg-light-green {
        background-color: #e6f9f0;
        color: #00b894;
    }

    .bg-light-blue {
        background-color: #e6f2ff;
        color: var(--brand-blue);
    }

    .value-card h4 {
        font-weight: 700;
        margin-bottom: 10px;
        font-size: 1.3rem;
    }

    .value-card p {
        font-size: 0.95rem;
        color: #777;
    }

    /* Mobile Responsive */
    @@media (max-width: 768px) {
        .about-hero {
            min-height: 200px;
            padding: 30px 15px;
        }

            .about-hero h1 {
                font-size: 2.2rem;
            }

        .rounded-img {
            height: 300px;
            margin-bottom: 30px;
        }

        .d-flex {
            flex-direction: column;
        }

        .btn-custom {
            width: 100%;
            text-align: center;
            margin: 0 0 10px 0;
        }

        .btn-outline {
            margin-left: 0;
        }
    }
</style>

<main>
    <section class="about-hero">
        <div class="container">
            <h1 class="display-4">Hành Trình <span class="highlight">Sip & Savor</span></h1>
            <p class="lead">Không chỉ là đồ uống, đó là nghệ thuật cá nhân hóa.</p>
        </div>
    </section>

    <section class="section-padding">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-lg-6">
                    <img src="https://images.unsplash.com/photo-1544148103-0773bf10d330?ixlib=rb-1.2.1&auto=format&fit=crop&w=1050&q=80" alt="About Sip & Savor" class="rounded-img">
                </div>
                <div class="col-lg-6 ps-lg-5 mt-4 mt-lg-0">
                    <span class="sub-heading text-uppercase">VỀ CHÚNG TÔI</span>
                    <h2 class="main-heading">
                        Tạo Ly Đồ Uống <br>
                        <span style="color: var(--brand-orange);">Hoàn Hảo</span>
                    </h2>
                    <p class="description-text">
                        Tại Sip & Savor, chúng tôi tin rằng mỗi người đều có một "gu" riêng biệt. Hệ thống của chúng tôi không chỉ bán đồ uống, mà còn cho phép bạn trở thành một nghệ nhân pha chế cho chính mình.
                    </p>
                    <ul class="feature-list">
                        <li><i class="fas fa-check-circle"></i> Nguyên liệu tươi mới mỗi ngày</li>
                        <li><i class="fas fa-check-circle"></i> Công thức độc quyền từ chuyên gia</li>
                        <li><i class="fas fa-check-circle"></i> Giao hàng nhanh, giữ trọn vị ngon</li>
                    </ul>
                    <div class="d-flex">
                        <a href="@Url.Action("Index", "Products")" class="btn-custom btn-orange">Khám phá Menu</a>
                        <a href="/Story" class="btn-custom btn-outline">Câu chuyện</a>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <section class="section-padding values-section">
        <div class="container">
            <div class="text-center mb-5">
                <h2 style="font-weight: 800; color: var(--text-dark);">Giá Trị Cốt Lõi</h2>
                <p class="text-muted">Điều làm nên sự khác biệt của chúng tôi</p>
            </div>
            <div class="row g-4">
                <div class="col-md-4">
                    <div class="value-card">
                        <div class="icon-box bg-light-orange">☕</div>
                        <h4>Chất Lượng</h4>
                        <p>Hạt cà phê và lá trà được tuyển chọn từ những nông trại tốt nhất, hương vị đậm đà nguyên bản.</p>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="value-card">
                        <div class="icon-box bg-light-green">🌿</div>
                        <h4>Tươi Mới</h4>
                        <p>Cam kết Farm-to-cup, không chất bảo quản. 100% nguyên liệu tự nhiên tươi mới mỗi ngày.</p>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="value-card">
                        <div class="icon-box bg-light-blue">💎</div>
                        <h4>Cá Nhân Hóa</h4>
                        <p>Công nghệ AI gợi ý đồ uống theo tâm trạng. Tự do điều chỉnh độ ngọt và topping theo ý thích.</p>
                    </div>
                </div>
            </div>
        </div>
    </section>
</main>